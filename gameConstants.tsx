import * as THREE from "three";

export const CANVAS_UNIT = 7;
export const CANVAS_UNIT_SIZE_SCALE = 1;

export const threeColorBlack = new THREE.Color(0, 0, 0);
export const threeColorWhite = new THREE.Color(22, 45, 67);

export type Size2D = {
	width?: number;
	height?: number;
};
